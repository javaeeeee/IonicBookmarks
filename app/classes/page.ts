/**
 * A class representing a page.
 * @author Dmitry Noranovich
 */
export class Page {
    /**
     * Page class constructor which defines two fields.
     * @argument The title of the page.
     * @argument The page component.
     */
    constructor(public title: string, public component: any) { }
}